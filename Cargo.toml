[package]
name = "magicfs"
version = "0.1.0"
edition = "2021"

[dependencies]
fuser = { version = "0.14", default-features = false }
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
